import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // La configuración se mantiene mínima a propósito.
  // Cada opción que se añada aquí debe justificarse (regla de calidad).
};

export default nextConfig;
